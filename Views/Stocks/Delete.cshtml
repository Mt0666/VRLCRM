@model StockItem
@{
  ViewData["Title"] = "Ürünü Pasife Al";
}

<div class="row">
  <div class="col-md-8 col-lg-6">
    <div class="card border-danger">
      <div class="card-body">
        <div class="d-flex align-items-start mb-4">
          <span class="badge badge-center rounded-pill bg-danger me-3 p-3">
            <i class="icon-base ri ri-error-warning-line icon-24px"></i>
          </span>
          <div>
            <h5 class="mb-2">Bu ürünü pasife almak istediğinize emin misiniz?</h5>
            <p class="mb-0 text-body-secondary">
              <strong>@Model.Name</strong> (@Model.StockCode) pasif duruma alınacak; kalıcı olarak silinmeyecektir.
            </p>
          </div>
        </div>

        <ul class="list-unstyled mb-4">
          <li class="mb-2"><strong>Kategori:</strong> @Model.Category.Name</li>
          <li class="mb-2"><strong>Fiyat:</strong> @Model.Price.ToString("N2") ₺</li>
          <li><strong>Stok Adedi:</strong> @Model.StockQuantity</li>
        </ul>

        <form asp-action="Delete" method="post">
          <input type="hidden" asp-for="Id" />
          <div class="d-flex gap-2">
            <button type="submit" class="btn btn-danger">
              <i class="ri-close-circle-line me-1"></i>Evet, Pasife Al
            </button>
            <a asp-action="Index" class="btn btn-outline-secondary">Vazgeç</a>
          </div>
        </form>
      </div>
    </div>
  </div>
</div>
